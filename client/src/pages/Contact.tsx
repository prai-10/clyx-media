import { useState } from 'react';
import { ArrowUpRight, Mail, MessageCircle, CalendarDays } from 'lucide-react';
import PageShell from '@/components/layout/PageShell';
import { Label, Section } from '@/components/ui/primitives';
import { safeHref, usePageContent } from '@/lib/pageContent';
export default function Contact(){const c=usePageContent('contact');const [sent,setSent]=useState(false);return <PageShell eyebrow={c.heroEyebrow} title={<>{c.heroTitle}<br/><span className="text-yellow">{c.heroHighlight}</span></>} intro={c.heroIntro}><Section><div className="grid gap-16 md:grid-cols-[.75fr_1.25fr]"><div><Label>{c.detailsLabel}</Label><div className="space-y-6 text-sm">{c.email&&<a href={`mailto:${c.email}`} className="flex items-center gap-3 hover:text-blue"><Mail size={18}/>{c.email}</a>}{c.whatsappUrl&&<a href={safeHref(c.whatsappUrl)} className="flex items-center gap-3 hover:text-blue"><MessageCircle size={18}/>{c.whatsappLabel}</a>}{c.calendlyUrl&&<a href={safeHref(c.calendlyUrl)} className="flex items-center gap-3 hover:text-blue"><CalendarDays size={18}/>{c.calendlyLabel} <ArrowUpRight size={16}/></a>}</div></div><form id="contact-form" onSubmit={e=>{e.preventDefault();setSent(true)}} className="grid gap-6"><input required placeholder={c.formName} className="border-b border-grid bg-transparent px-0 py-4 text-lg outline-none focus:border-blue"/><input required type="email" placeholder={c.formEmail} className="border-b border-grid bg-transparent px-0 py-4 text-lg outline-none focus:border-blue"/><input placeholder={c.formCompany} className="border-b border-grid bg-transparent px-0 py-4 text-lg outline-none focus:border-blue"/><textarea required placeholder={c.formMessage} rows={4} className="resize-none border-b border-grid bg-transparent px-0 py-4 text-lg outline-none focus:border-blue"/><button className="inline-flex w-fit items-center gap-3 bg-yellow px-6 py-4 text-sm font-semibold uppercase tracking-[.1em] text-dark hover:bg-blue hover:text-white">{sent?c.formSent:c.formButton} <ArrowUpRight size={16}/></button></form></div></Section><section className="bg-yellow text-dark"><div className="container grid gap-8 py-20 md:grid-cols-3 md:py-28">{[c.step1,c.step2,c.step3].map((step,i)=><div key={i}><p className="font-mono text-xs">{String(i+1).padStart(2,'0')}</p><h2 className="display mt-5 text-4xl font-bold">{step}</h2></div>)}</div></section></PageShell>}
